entrypointDiscovery:
- type: kustomize
  regex: home/(?P<name>[^/]+)/overlays/(?P<overlay>[^/]+)
  context:
    policies:
    - test.test
    - test.test2
    - core.kubernetes.generic
    - core.from.sancire.config
    
