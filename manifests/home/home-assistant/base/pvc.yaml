apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-config-data
spec:
  resources:
    requests:
      storage: 100G
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
