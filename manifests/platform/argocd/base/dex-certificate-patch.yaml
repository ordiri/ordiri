apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-dex-server
spec:
  template:
    spec:
      containers:
      - name: dex-server
        volumeMounts:
          - name: ca-certs
            mountPath: /etc/ssl/certs
      volumes:
        - name: ca-certs
          secret:
            secretName: vault-ca